server:
  port: 8081

spring:
  application:
    name: accounts
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
    driverClassName: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

hibernate:
  dialect: org.hibernate.dialect.PostgreSQLDialect
  show_sql: true

client:
  notifications:
    base-url: ${CLIENT_NOTIFICATIONS_URL}
    connect-timeout-ms: 1000
    response-timeout-ms: 1000

management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      show-details: always

#server:
#  port: 8081
#
#spring:
#  application:
#    name: accounts
#  datasource:
#    url: jdbc:postgresql://bank-db:5432/bank-db
#    username: postgres
#    password: postgres
#    driverClassName: org.postgresql.Driver
#  jpa:
#    hibernate:
#      ddl-auto: update
#    show-sql: true
#
#hibernate:
#  dialect: org.hibernate.dialect.PostgreSQLDialect
#  show_sql: true
#
#client:
#  notifications:
#    base-url: http://notifications/notifications
#    connect-timeout-ms: 1000
#    response-timeout-ms: 1000