server:
  port: ${SERVER_PORT:8085}

spring:
  application:
    name: exchange-generator
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        hostname: 127.0.0.1
      config:
        enabled: false
  security:
    oauth2:
      resource-server:
        jwt:
          issuer-uri: http://localhost:8180/realms/master

