server:
  port: 8087

spring:
  application:
    name: transfer

client:
  accounts:
    base-url: ${CLIENT_ACCOUNTS_URL}
    connect-timeout-ms: 1000
    response-timeout-ms: 1000
  blocker:
    base-url: ${CLIENT_BLOCKER_URL}
    connect-timeout-ms: 1000
    response-timeout-ms: 1000
  notifications:
    base-url: ${CLIENT_NOTIFICATIONS_URL}
    connect-timeout-ms: 1000
    response-timeout-ms: 1000
  exchange:
    base-url: ${CLIENT_EXCHANGE_URL}
    connect-timeout-ms: 1000
    response-timeout-ms: 1000

