server:
  port: 8086

spring:
  application:
    name: notifications

management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      show-details: always