server:
  port: ${SERVER_PORT:8086}

spring:
  application:
    name: notifications
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        hostname: 127.0.0.1
      config:
        enabled: false