server:
  port: 8082

spring:
  application:
    name: blocker
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        hostname: 127.0.0.1
      config:
        enabled: false