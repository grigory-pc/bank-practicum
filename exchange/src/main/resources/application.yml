server:
  port: 8084

spring:
  application:
    name: exchange
  cloud:
    consul:
      host: consul
      port: 8500
      config:
        enabled: false

client:
  exchange-generator:
    base-url: http://exchange-generator/exchange-generator
    connect-timeout-ms: 1000
    response-timeout-ms: 1000

